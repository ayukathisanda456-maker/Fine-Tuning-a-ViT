# ViT - brief theoretical introduction and fine-tuning with CIFAR-10
In this project we use a pre-trained ViT(pre-trained by OpenAI) with CIFAR-10 dataset. The outcome was to observe the change in the accuracy of the model as a function of the number of samples and number of training epochs. 
